# spotify-client

CI workflow badge (replace `<OWNER>` and `<REPO>` with your values):

![CI](https://github.com/<OWNER>/<REPO>/actions/workflows/ci.yml/badge.svg)

Quick commands

```bash
# install deps
npm install

# run tests
npm test

# run tests with coverage
npm run test:cov

# run linter
npm run lint
```

Notes
- Update the badge URL above to point to your GitHub repository to display status.
- CI uploads a `coverage/` artifact after running tests with coverage.
