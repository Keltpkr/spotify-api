{
  "name": "spotify-client",
  "version": "0.1.0",
  "description": "A modular SpotifyClient for Node.js (CommonJS) - Client Credentials flow (Phase 1)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest",
    "test:watch": "jest --watchAll",
    "test:cov": "jest --coverage --silent",
    "lint": "eslint . --ext .js"
  },
  "keywords": [
    "spotify",
    "client",
    "spotify-api",
    "node"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.5.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
